POST http://localhost:3001/api/degrees/create_studyplan
Content-Type: application/json

{
    "degree_id": "2",
    "name": "Test plan 11200",
    "uid": "root"
}

//gives an error, if name has been added already
//change name to test addind for first time, if needed